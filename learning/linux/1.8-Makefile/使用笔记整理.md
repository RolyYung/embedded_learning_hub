# wildcard 关键字
***
`wildcard: 通配符`
在计算机中, `wildcard` 几乎所有语言里都会有这个概念, 斗地主里, 我们打牌会用到的`赖子` 也可以用这个词. 
中文翻译是通配符
***
在`Makefile`中 这是一个关键字. 使用示例
`OBJS = $(wildcard *.o)`
配合`$()`使用, `wildcard`后接通配符语句, 这个意思就是 我后面是通配符语句, 你要根据通配符规则找到对应的文件.