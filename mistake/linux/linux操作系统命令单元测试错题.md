![[file-20260902192135197.png]]

```txt
[] 通配符匹配括号内一个字符
* 通配符匹配任意字符
做错的原因是漏掉了后面的*符
```


![[file-20260902192504812.png]]


```txt
还是老毛病没有看指令
usermod -g 更改用户的group
groupmod 直接改group 不需要-g 选项
在groupmod命令中 -g选项是指定GID
```

![[file-20260902192702952.png]]

```markdown
# 很明显的知识漏点
`->`是很明显的软链接特征
硬链接和普通文件无异, 不会有`->`符号
```

