# `TRUNC`
`truncate` 截断的意思
想象成一把剪刀, 就是负责截断的意思. 
在`linux` 文件IO函数 open下, 就是指截断成0, 也就是清空文件的意思

# `EXCL`
`exclusive`独立的 排他的. 独家的
在open函数下, 就是如果不是我创建的 那就不创建了, 直接报错退出.

# `vector`
在C语言中指的是数组. 
在数学领域指的是向量. 这要区分开来.